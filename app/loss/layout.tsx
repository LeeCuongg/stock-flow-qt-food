import { DashboardLayout } from '@/components/dashboard-layout'

export default function LossLayout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout title="Hao hụt">{children}</DashboardLayout>
}
